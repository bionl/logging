params {
    outdir = './results'
}

profiles {
    test {
        docker.enabled = true
    }
    docker {
        docker.enabled = true
    }
}

timeline {
    enabled = true
    file = "${params.outdir}/timeline.html"
    overwrite = true
}

